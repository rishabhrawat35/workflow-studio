Round: 0 of 3 (the writer's own self-check after finishing the draft; no fresh context existed, so the second review is still open and is stated as such in the owner note)
Takeaways: 1. Workflow Studio is a three-workflow framework (define, foundation, deliver) in which one PM and one AI ship a product, and `orchestrate.py` refuses to move a change past a gate without its artifact. 2. Trying it costs a copy of `framework/` plus `workflows/`, PyYAML and one of five AI tools; `orchestrate.py check` prints CLOSED and the slash commands mirror GitHub Spec Kit. 3. Every gate and refusal in the README was exercised in a real walk on 2026-09-10; `tools/check_all.py` proves the repository and 207 test cases cover the workflows.
Scanner lost at: nowhere; the title, tagline, bold sentence, badges, status line and map image state what it is, who it is for and that it was checked on 2026-09-10. / Evaluator lost at: nowhere; Key features, The three workflows and the first command sit within the first 60 lines. / Operator lost at: nowhere reached in the draft; the worked example carries every command with its output, and Troubleshooting maps the seven errors met during the run to fixes.
Verification: commands re-run — `tools/check_all.py`, the quickstart (`cp`, `orchestrate.py check`, `install_commands.py --agent claude`), the 13 `orchestrate.py` calls of the walk, `commit.py --dry-run`, `commit.py`, `tasks_to_issues.py --dry-run`, the missing-workflows and missing-PyYAML errors, `install_commands.py --agent vim`; outputs match — every block was pasted from the scratch run of 2026-09-10 with whitespace trimmed only (the `check_all.py | tail -5` block was corrected once after a first paste carried a duplicated line); counts match — 51 steps (18 + 14 + 19 in `workflows/*.yaml` and `framework/steps.md`), 21 commands (`framework/commands/`, `commands.yaml`, `check` output), 8 entry commands (`entry: true`, corrected from a first draft's "seven"), 12 component files, 105 files from `--agent all`, 207 test cases in 17 lettered sections (A–M, O–R; N is the re-run), 6 refusals shown, 8 graph rules in SCHEMA.md; links resolve — `lint.py` checked every relative link, image and anchor; secrets none (the repository has no secret files and the README names no variable value).
- [keep] Hero block, Status line, map screenshot, first command — takeaway 1 and the "is this maintained?" doubt.
- [keep] Audience redirect, For AI agents — the second reader (the operating AI).
- [keep] Key features, The three workflows, Main path of a request — takeaway 1; the Mermaid labels are the step ids from `workflows/*.yaml`.
- [keep] Installation, Quickstart — takeaway 2; the five-tool table replaces the "Supported platforms" row.
- [keep] Worked example — takeaway 3; six refusals and the one commit, each output produced by the command shown above it.
- [keep] Reference tables (21 commands, 51 steps, 9 tools) inside `<details>` — the Operator's lookup.
- [keep] How it works, Tests and evaluation, Troubleshooting, Customizing the workflows, Limits and non-goals — takeaways 1 and 3.
- [keep] Further documentation, Support and contributing, Acknowledgements, License — the public repository's obligations.
- [cut] (applied) Comparison with alternatives, Glossary, Configuration, Step-by-step walkthrough, Technology stack, FAQ, Key numbers — each duplicated a kept section or restated `framework/README.md`, and the body was over the 1,500-word target before the cut.
- [prove] (applied, A10) "seven entry commands" — `commands.yaml` carries eight `entry: true` lines; reworded to "eight".
- [prove] (applied, A8) the `check_all.py | tail -5` block — re-run and replaced with the verbatim five lines.
- [reword] (applied, A1) "The gates are enforced by a tool, not by a prompt." — shortened to "A tool enforces the gates." to keep a subject and a verb in the lead-in.
- [restructure] (applied, A4) the four pasteable requests in the worked example — moved from a sentence into a two-column table.
- [restructure] (applied, A4) Further documentation — eight parallel links moved from bullets into a Document/Content table.
Score: A1 pass (every bullet, lead-in, heading and prose cell carries a subject and a verb; single-term cells such as "none" and "PM" are exempt) · A2 pass (`lint.py` OK; no rhetorical-question heading; no casual opener) · A3 pass (the reader is anyone opening the repository; the AI agent is the justified second reader, redirected under the introduction) · A4 pass (the tools, the five AI tools, the pasteable requests, the errors and the documents are tables; the features, the mechanism, the agent rules and the limits are lists) · A5 pass (each number serves a decision: 51/21 prove closedness, 207 answers "is it tested?", 105 tells the reader what `--agent all` writes, the date answers "is it maintained?"; no size, line-count or byte column) · A6 pass (each heading and diagram label reads alone; diagram labels are step ids) · A7 pass (`orchestrate.py`, `studio.html`, `check_all.py`, `install_commands.py`, `commit.py`, `tasks_to_issues.py`, the lane, the framing and the change record are each described in the sentence that first names them) · A8 pass (`python3 lint.py README.md` → OK, 25 headings, 1500 body words) · A9 pass against the owner's target for a framework (1,500 words, inside the 900–1,500 target; the 400–900 band of CHALLENGER.md applies to a small tool and the owner set the wider band for this repository) · A10 pass (SKILL.md, CHALLENGER.md, the two scripts and the README agree on 51 steps, 21 commands, 8 entry commands, 207 cases and 67 inventory lines)
Must-fix findings: 0 open (three were found and applied during this self-check: the entry-command count, the `check_all.py` output block, and the lead-in rewording)

---

Round: 1 of 3 (second reviewer, fresh context; the repository, `readme-inventory.md` and CHALLENGER.md were the only inputs)
Takeaways: 1. Workflow Studio is three YAML workflows (`define`, 18 steps; `foundation`, 14; `deliver`, 19) in which one PM and one AI ship a product, and `framework/tools/orchestrate.py` refuses to move a change record past a PM gate or a challenger step without the artifact that step requires. 2. Trying it costs a copy of `framework/` with `workflows/` inside it, PyYAML, git and one of five AI tools; `orchestrate.py check` prints `CLOSED — 51 steps … 21 commands` and the slash commands carry GitHub Spec Kit's names and folders. 3. The refusals are real: six were reproduced from a fresh product folder, `tools/check_all.py` prints `ALL CHECKS PASSED`, and `framework/test-cases.md` holds 207 cases.
Scanner lost at: nowhere; the title, tagline, bold sentence, four badges, status line with the date and the map screenshot say what it is, who it is for and that it was checked on 2026-09-10. / Evaluator lost at: "Commands carry GitHub Spec Kit's names" in Key features, the first mention of Spec Kit, which is not linked or described until Reference tables 200 lines later; the rest of the first 60 seconds (Key features, the workflows table, the first command) holds. / Operator lost at: the start of the worked example, where the first two output blocks are shorter than what the command prints (see the [prove] finding), so an operator comparing their terminal with the README sees extra lines and doubts the "verbatim" claim; every later block matches to the character.
Verification: commands re-run — in `scratchpad/ws-chal/`: `tools/check_all.py | tail -5`, `lint.py README.md`, the hero and Linux/macOS install (`cp`, `git init`, `orchestrate.py check`), `install_commands.py --agent claude`, `--agent all` (105 files counted on disk), `--agent vim`, the missing-`workflows/` refusal, all 13 `orchestrate.py` calls of the walk (`start`, `advance` ×10, `rerun`, the fresh `start --command speckit.implement`), `commit.py --dry-run`, `commit.py`, `tasks_to_issues.py --dry-run`; outputs match — every refusal line, the `check` line, the installer lines, the `check_all.py` tail and the `commit.py` block match verbatim (the commit SHA and timestamps differ by run, as expected); two blocks do not match: the `start` block shows 3 of the 9 lines the command prints (`inputs:`, `PM gives:`, `does:`, `produces:`, `exits:` are missing) and the `advance --to sort --answer` block omits the two `last edges:` lines the command prints; counts match — 51 steps (18 + 14 + 19 counted in `workflows/*.yaml`), 21 commands (`framework/commands/`, `commands.yaml`, the `check` line), 8 `entry: true` commands, 6 roles and 4 modes in `roles.yaml`, 12 component files each carrying the five shared sections, 105 installed files, 6 refusals in the worked example, 8 graph rules in SCHEMA.md, 25 headings and 1,500 body words from `lint.py`, 17 table-of-contents entries against 17 H2 sections below it, 67 inventory lines; one count does not match — `framework/test-cases.md` has 18 lettered sections (A–R), not 17; 207 is the row count of A–M and O–R, and section N holds 45 re-run rows of those same cases (252 rows in all); links resolve — `lint.py` resolved every relative link, image and in-file anchor, and `SCHEMA.md#graph-rules-validator-enforced` was checked by hand against the heading "Graph rules (validator-enforced)"; both PNGs open and show what their alt text says; secrets none (no token, key or credential value appears in the README or the inventory; the inventory reports 0 secret files).
- [reword] (must-fix, A1) "Four more requests for the same machinery:" (Worked example, line 142) — a fragment with no verb; replace with "Four more requests reach the same machinery through these commands:".
- [restructure] (must-fix, A4) "The framework needs Python 3.10 or newer with PyYAML (every tool imports `yaml`; nothing declares it), git, and one of the five supported AI tools; `gh` serves only `/speckit-taskstoissues`." (Installation) — five parallel requirements, two with sub-parts, in one 40-word sentence; replace with a list or a Requirement/Why table: Python 3.10 or newer — `tools/validate.py` uses `str | None` annotations; PyYAML — every tool imports `yaml` and nothing declares it; git — `commit.py` refuses to run outside a git repository; one of the five AI tools in the table below — runs the slash commands; `gh` (optional) — serves only `/speckit-taskstoissues`.
- [prove] (must-fix, A5) "207 cases in 17 lettered sections, each with a verdict and a step path" (Tests and evaluation) — the file has 18 lettered sections A–R; 207 counts sections A–M and O–R while section N re-runs those cases in 45 rows; and cases with the verdict GAP or PARTIAL (A6, A10, B10 and others) carry a reason, not a step path. Reword to "207 cases in sections A–M and O–R, each with a verdict and, where it passes, its step path; section N re-runs every case against the current workflows".
- [prove] (must-fix, A10) "every output block is verbatim" (Worked example) against the `start` block and the `advance --to sort` block — `orchestrate.py start` prints nine lines (adding `inputs:`, `PM gives:`, `does:`, `produces:` and `exits:`) and `advance --to sort --answer …` ends with two `last edges:` lines; the README shows 3 and 21 of those lines. Either paste the full blocks or reword the claim to "every refusal and result line is verbatim; the two `next` printouts are cut after the lines the text discusses" and mark the cuts with a `…` line inside each block.
- [reword] (should-fix) "Commands carry GitHub Spec Kit's names." (Key features) — Spec Kit is named here first and linked only under Reference tables; link it here ([GitHub Spec Kit](https://github.com/github/spec-kit)) and add "the command vocabulary of GitHub Spec Kit" so the term is introduced before use (R6).
- [reword] (should-fix) the Purpose cells for `orchestrate.py`, `tasks_to_issues.py` and `commit.py` in "The tools and their refusals" — the other six rows lead with a verb ("writes", "generate", "checks", "embeds", "generates", "runs") and these three are noun phrases; rewrite as "runs `check`, `start`, `next`, `advance` and `rerun` on a change record", "creates one GitHub issue per task line and writes the number back; `--dry-run` is the default without `gh`", "makes the one code commit: regenerates the indexes, sets the node status and moves the record to `deliver.done`".
- [restructure] (should-fix) "`tools/check_all.py` proves this repository in one run: the validator, the builder, the docs generator, the offline check on `studio.html`, `orchestrate.py check`, and the product-side generators on a fixture holding one broken decision file." (Tests and evaluation) — six parallel checks in one sentence; a numbered list in the order the tool runs them tells the operator which check failed when the output stops early (R3).
- [restructure] (should-fix) Quickstart step 6 "Try the running product, answer `/speckit-accept accepted`, then run `/speckit-git-commit` …" — three actions in one step (R9); split into step 6 (try the product and answer `/speckit-accept accepted`) and step 7 (run `/speckit-git-commit` for the one commit).
- [reword] (should-fix) "and nothing is done before `deliver.accept`" (For AI agents, last paragraph) — ambiguous; the YAML and AGENTS.md mean "nothing is marked done"; replace with "and no record is marked done before `deliver.accept`".
- [reword] (should-fix) "`framework/orchestration/commands.yaml` plus `framework/commands/speckit.status.md` or its sibling" (Customizing the workflows) — "or its sibling" does not name the file; replace with "plus the matching template `framework/commands/speckit.<name>.md`" written with a real name, e.g. "plus the command's template under `framework/commands/`, such as `speckit.status.md`".
- [cut] (should-fix) "`build/` (gitignored), `workflows/.gitkeep`, `.gitignore` and `__pycache__/` are left unlinked on purpose." (Further documentation) — a note to the reviewer, not to the reader; it changes no reader decision (R4, R8); move the sentence to the owner note.
- [reword] (should-fix) "the PM gates (✔) are …" (Main path of a request) and the "✔" inside the three Mermaid gate labels — the form rule allows emoji on H2 headings only or nowhere; write "the three PM gates are …" and drop the mark from the labels, or keep the mark in the diagram only and say "marked ✔ in the diagram" once.
- [add] (should-fix) `framework/commands/` — link it from "Slash commands for one AI tool", where the reader learns the 21 templates live there.
- [add] (should-fix) `studio.html` — link it from the introduction sentence "`studio.html` draws them" (or from "The renderer works offline"), since it is the file the reader opens to see the workflows.
- [add] (should-fix) `framework/templates/CLAUDE.md` — name it beside `AGENTS.md` in the Further documentation row (it is one line, `@AGENTS.md`, and the install step copies it).
- [keep] Hero block, badges, status line, map screenshot, first command, audience redirect — takeaways 1 and 2, and the "is it maintained?" doubt.
- [keep] Key features, The three workflows, Main path of a request, How it works — takeaway 1; the six feature claims are each backed by the walk, the YAML or a linked file.
- [keep] Installation, Quickstart — takeaway 2; the five-tool table and the `--agent all` count were reproduced.
- [keep] Worked example — takeaway 3; six refusals and the one commit, each reproduced.
- [keep] Reference tables (21 commands, 51 steps, 9 tools, all inside `<details>`) — the Operator's lookup; every row checked against `commands.yaml`, the three YAML files and the tool docstrings.
- [keep] Tests and evaluation, Troubleshooting, For AI agents, Customizing the workflows, Limits and non-goals — takeaway 3 and the second reader; all seven Troubleshooting symptoms were reproduced.
- [keep] Further documentation, Support and contributing, Acknowledgements, License — the public repository's obligations.
Score: A1 fail (one fragment, line 142) · A2 pass (`lint.py` OK; no casual opener; no question heading) · A3 pass (one PM and one AI is the repository's own design, stated in `framework/README.md`) · A4 fail (the Installation requirements sentence) · A5 fail (the "17 lettered sections" count and the "each with a step path" claim are not true of `framework/test-cases.md`) · A6 pass (every heading, `<summary>` and diagram label reads alone; diagram labels are step ids) · A7 pass (every tool, file and term is described in the sentence that first names it, except Spec Kit, listed as should-fix) · A8 pass (`python3 lint.py README.md` → OK, 25 headings, 1500 body words; every link, image and anchor resolves) · A9 pass (1,500 body words, at the top of the 900–1,500 band set for a framework) · A10 fail (the README's `start` and `advance` output blocks disagree with what `orchestrate.py` prints while the text calls them verbatim)
Must-fix findings: 4, listed above with the criterion each fails (A1 fragment; A4 Installation requirements; A5 test-case section count; A10 truncated output blocks). Round 1: FAIL.

---

Round 2 — writer (findings of Round 1 applied; `python3 lint.py README.md` → `OK — README.md: 25 headings, 1498 body words; every link and anchor resolves.`)
- [reword] (must-fix, A1) "Four more requests for the same machinery:" — applied; now "Four more requests reach the same machinery through these commands:".
- [restructure] (must-fix, A4) the Installation requirements sentence — applied; now a Requirement/Why table with the five rows the reviewer named (Python 3.10+, PyYAML, git, one of the five AI tools, `gh` optional), preceded by "No package exists … The framework needs five things:".
- [prove] (must-fix, A5) "207 cases in 17 lettered sections, each with a verdict and a step path" — applied; now "207 cases in sections A–M and O–R, each with a verdict and, where it passes, its step path; section N re-runs every case against the current workflows".
- [prove] (must-fix, A10) the truncated `start` and `advance --to sort` blocks — applied by re-running both commands on 2026-09-10 in a fresh product folder (`scratchpad/ws-skill/my-product-r2/`) and pasting the full nine-line `start` output and the full `advance` output including `does:`, `produces:` and the two `last edges:` lines; the timestamps in these two blocks read 11:17 where the later blocks read 11:02/11:03, because the later blocks come from the first walk.
- [reword] (should-fix) Spec Kit introduced before use — applied; the Key features lead-in now reads "Commands carry the command vocabulary of [GitHub Spec Kit](https://github.com/github/spec-kit)."
- [reword] (should-fix) Purpose cells of `orchestrate.py`, `tasks_to_issues.py`, `commit.py` — applied with the reviewer's verb-led wording.
- [restructure] (should-fix) the `check_all.py` sentence — applied; now a six-item numbered list in run order.
- [restructure] (should-fix) Quickstart step 6 — applied; split into step 6 (try the product, answer `/speckit-accept accepted`) and step 7 (`/speckit-git-commit`).
- [reword] (should-fix) "nothing is done before `deliver.accept`" — applied; now "no record is marked done before `deliver.accept`".
- [reword] (should-fix) "or its sibling" — applied; now "plus the command's template under `framework/commands/`, such as `speckit.status.md`".
- [cut] (should-fix) the "left unlinked on purpose" sentence — applied; removed from the README and moved to the owner note.
- [reword] (should-fix) the ✔ marks — applied; the sentence now reads "the three PM gates are …" and the three Mermaid labels carry no mark; the diagram's gates are the three diamond nodes named in that sentence.
- [add] (should-fix) link to `framework/commands/` — applied under "Slash commands for one AI tool".
- [add] (should-fix) link to `studio.html` — applied in the introduction sentence.
- [add] (should-fix) `framework/templates/CLAUDE.md` — applied in the Further documentation row beside `AGENTS.md`, with its one-line content named.
Rejected: none.
Word budget: the full output blocks added no body words (code blocks are exempt); the requirements table and the `check_all.py` list moved 40 words into a table and added 45 in a list, so 60 words were cut from Key features, How it works, Quickstart step 1, the worked-example prose and Limits to hold the body at 1,498 words (A9).
Must-fix findings open: 0. Round 2 ready for the reviewer's re-score.

---

Round 2 — challenger (fresh read of README.md after the writer's 15 changes)
Verification: `python3 lint.py README.md` → `OK — README.md: 25 headings, 1498 body words; every link and anchor resolves.`; the re-pasted `start` and `advance --to sort --answer …` blocks were re-run in a fresh product folder (`scratchpad/ws-chal/r2/`) and match line for line apart from the `since` timestamp; the Installation table, the six-item `check_all.py` list and the new links (`studio.html`, `framework/commands/`, `framework/templates/CLAUDE.md`) resolve and match the repository; the Mermaid block carries no ✔; every other output block is unchanged from Round 1, where it matched verbatim; counts unchanged and re-checked — 51 steps, 21 commands, 8 entry commands, 105 files, 6 refusals, 207 cases in A–M and O–R with N as the re-run, 17 ToC entries against 17 H2 sections; secrets none.
Round 1 findings: all 15 applied as stated in the writer's note; each replacement was read in place and none introduced a fragment, a casual opener, a question heading, a banned word, a narrowed audience, a prose enumeration of three or more items, a valueless number or an unintroduced term.
Owner's standing complaints, checked once more across the whole draft: casual openers none; fragments none (every bullet, lead-in, list item and prose cell carries a subject and a verb; noun-headed columns such as Refuses, Why and Content frame their cells); narrowed audience none (one PM and one AI is the repository's design); prose where a list belongs none (the requirements and the checks are now a table and a list); valueless numbers none (each count serves a decision: 51/21 prove closedness, 207 answers "is it tested?", 105 tells the reader what `--agent all` writes, the date answers "is it maintained?"); unclear names none ("or its sibling" is gone); question headings none; filler none (`lint.py` banned list clean).
- [reword] (should-fix) "they ran on 2026-09-10 in a fresh product folder, and every output block is verbatim" (Worked example) — true, but the first two blocks now read `11:17` while the later blocks read `11:02`/`11:03`, so an attentive reader sees two runs; add "in two runs of the same walk" or re-paste all blocks from one run. No criterion fails.
- [keep] every section — the assignment to the three takeaways from Round 1 stands; the 60 words cut to hold A9 removed no claim a reader acts on (the dropped "one change in flight per node" limit remains stated in [framework/README.md](framework/README.md), linked under Further documentation).
Score:
| Criterion | Result | Evidence |
|---|---|---|
| A1 | pass | zero fragments; the Round 1 fragment is now a sentence |
| A2 | pass | no casual opener, no question heading, `lint.py` banned list clean |
| A3 | pass | audience is anyone opening the repository; the PM/AI pair is justified by `framework/README.md` |
| A4 | pass | requirements are a table, the six checks a list; no enumeration of three or more items left in prose |
| A5 | pass | every number serves a named decision and matches the repository, including the corrected test-case count |
| A6 | pass | every heading, `<summary>`, step name and diagram label reads alone |
| A7 | pass | Spec Kit is now introduced and linked at first mention; every file, command and term is described before use |
| A8 | pass | `lint.py` OK; every relative link, image and anchor resolves |
| A9 | pass | 1,498 body words, inside the 900–1,500 band set for a framework |
| A10 | pass | every output block matches what the tools print; every count agrees across README, inventory and repository |
Must-fix findings: 0. Should-fix findings: 1 (the two-run timestamp note). Round 2: PASS — the README may ship after the writer applies or rejects the one should-fix in the owner note.

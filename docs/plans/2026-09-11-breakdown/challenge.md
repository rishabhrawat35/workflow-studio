# Phase 6 — Challenger report (2026-09-11)

Fresh, adversarial pass over Phases 1–5. Every item below was tried with real commands in a scratch product built by `tools/new_product.py … --agent claude --no-git` under the session scratchpad `p6/p` (`architecture.md` filled; `code/backend/auth/auth.py` tagged `@node login`, `code/backend/notify/deep/x.py` tagged `@node notification-settings`, `code/tests/test_auth.py` tagged `@node reset-password`), first against the Phase 5 engine to confirm the defect, then against the fixed one. Confirmed defects were fixed in place within each phase's scope: `framework/tools/orchestrate.py` (Phase 2 scope, 1172 → 1295 lines), one sentence in `workflows/define.yaml` (`define.challenge`, Phase 1 scope; `build.py` and `docs.py` re-run), the cases added to `tools/check_all.py` step 8 (127 → 207 `==` lines; Phase 4 scope) and to `framework/test-cases.md` §T as T91–T170, the conventions in `framework/components/change-record.md`, `framework/README.md` and `framework/orchestration/README.md`, the counts in `README.md`, and a paragraph in `DECISIONS.md` (Phase 5 scope). No other agent was available, so the challenger fixed what it found.

Verdicts: **defect fixed** · **refused correctly** (already held) · **prose-only** · **accepted risk**. Must-fix findings open at the end: **0**.

| # | Attack | Verdict | What was seen, and what changed |
|---|---|---|---|
| 1a | Direct `advance --to define.discuss` (and `--to write`, `--to 1`) on a child at `waiting` while chunk 1 is at `define.discuss` | refused correctly | `refused: chunk 2 waits on chunk 1 (… is at define.discuss, not deliver.done)`; `--answer` on a waiting child refused ("released, not answered"). |
| 1b | `step: waiting` edited by hand to `define.discuss` (`mode: pm`), then `advance --to write --when 1 --answer "a page"` | **defect fixed** | Before: `next` showed `define.discuss`, `advance` moved it to `define.write`, the group block listed chunk 2 "in flight" beside chunk 1 — the dependency was skipped. Fix: `check_parent` (called by `next`, `advance`, `check <child>`) refuses a child at any open step whose dependencies still block it: "… at define.discuss, but it depends on chunk 1 (… at define.discuss, not deliver.done); a chunk leaves `waiting` only by `advance --to define.discuss` once every dependency is delivered or dropped — the record was edited by hand; set `step: waiting` and `mode: waiting` back". Closed chunks and `handed-over` are exempt. T98–T100. A corollary closed with it: `spawn` accepted a chunk that depended on a *later* chunk, so chunk 1 could start at `discuss` with an unmet dependency; now "dependencies first" is enforced at `breakdown` and `spawn` (T159). |
| 1c | Two `run.py --group` processes started at once on a group whose chunk 1 had just reached `deliver.done` | accepted risk | Both printed `group: released chunk 2` (both `advance` calls read `waiting` before either wrote), then both ran chunk 2 to its PM stop. End state consistent: chunk 2 at `define.discuss` with **one** release edge in its history (the second write repeated the first), chunk 3 still waiting, no traceback. Two runners on one group is not a supported mode; there is no file lock and adding one is out of this plan's scope. The state converges because every move is a full-file write of the same content. |
| 2 | `spawn` run twice on the same parent (its `step:` set back to `define.spawn` by hand, `advance --to broken-down`) | **defect fixed** | Before: no duplicate child files (matched by slug) but a second decision `D-0002` superseding `D-0001` for the same set, and chunk 1 re-entered `define.discuss` with a second `define.spawn → define.discuss` edge and a new `since`. Fix: `plan_spawn` counts the parent's `confirm-breakdown → spawn` edges against its `spawn → broken-down` edges; every yes allows exactly one spawn: "the breakdown confirmed in the last `## Breakdown — PM answer` … was already spawned (1 spawn(s) for 1 yes(es) in the history; its group block is in changes/QUEUE.md); spawn runs once per confirmation — to change the chunks, use revise-breakdown from a chunk still in Define". T101–T102. |
| 3 | Parent re-confirmed (`revise-breakdown` from chunk 3, PM yes, re-spawn) while chunk 2 is at `deliver.execute` with an approved plan | **defect fixed** | Before: the re-spawn set chunk 2 back to `define.discuss` (`child_state` rewrote every kept chunk not delivered or dropped), and a proposal that dropped it would have moved it to `define.dropped` from a Deliver step. Fix: a chunk in Deliver is treated like a delivered one — file, history and state untouched, only its number and `depends_on` follow the confirmed order; `plan_spawn` refuses, before anything is written, a revision that removes a chunk mid-Deliver ("a revision never rewrites or removes a chunk being built — keep it in the proposal, wait for it to reach deliver.done, or drop it at its own gate") or makes it depend on an undelivered chunk ("a chunk being built cannot wait"). The parent then stays at `define.spawn` — safely paused — and the same yes spawns once the chunk closes (a delivered chunk no longer in the set is left as it is). A kept chunk still in Define (write … commit) is re-discussed as before, its body and history kept; documented. T116–T133. Found on the way: `child_discussion()` returned the *first* `## define.discuss` section, so a chunk re-discussed after a revision would have reopened the parent with its stale first discussion — now the newest section (T125). |
| 4 | `revise-breakdown` from a chunk at `deliver.done` | refused correctly | `refused: chunk 1 of … is already delivered (deliver.done); a delivered chunk is never revised — revise the breakdown from a chunk still in Define`. |
| 5 | PM answers at `confirm-breakdown`: a proposal that parses to zero chunks; "yes but rename chunk 2"; "Yes."; "oui" / "ja" / "haan" | refused correctly, message improved | Zero chunks: leaving `breakdown` is refused ("lists no chunks"), and a hand-emptied block is refused again at `spawn`. "yes but rename chunk 2", "yes, rename chunk 2", "yes please", "sure", "yep" are not a yes: `--to spawn` refused, `--to breakdown` records them for the rewrite. "Yes.", "YES!", `"yes"`, "confirmed.", "agreed as proposed" are a yes (T96). A non-English yes is refused as not a yes; the refusal now lists the accepted words ("yes, y, ok, okay, confirm, confirmed, agreed, or "yes, these chunks in this order" (punctuation allowed, nothing else)") so the PM knows what to type. Extending the list to other languages is a product choice left to the owner. |
| 6 | Slug collisions: a chunk slug equal to another group's child; equal to a plain record (`2026-09-11-9-hr-streak.md` vs chunk `hr-streak`); two chunks with one slug in one proposal | refused correctly | "chunk 2 (hr-streak) collides with the existing record changes/2026-09-11-9-hr-streak.md (same slug); choose another slug"; "two chunks share the slug 'hr-log' in …; every chunk needs its own slug" (now also refused at `breakdown`, before the PM reads the block). Across groups: proven in Phase 2 §10 and unchanged. |
| 7 | `changes/QUEUE.md` edited by hand: the `Parent:` line removed and a table row replaced by `| garbage`; another block's closing fence removed; the file deleted | **defect fixed** (fence), the rest held | The queue is never read for state, so a mangled row or a removed `Parent:` line is regenerated at the next move and nothing crashes. A removed closing fence, however, made the block pattern `<!-- group: X -->.*?<!-- /group -->` run on to the *next* group's fence: the next move rewrote both blocks as one (the other group's block was lost until its own next move). Fix: the pattern never crosses another `<!-- group:` marker; a block with no closing fence is rewritten up to the next group (or the end) and the move prints "changes/QUEUE.md: the group block for … had no closing fence (edited by hand); rewritten" — the file is named. A deleted `QUEUE.md` is recreated with the moving group's block (the others return at their next move). T161–T162. Found on the way: `advance` printed its first line between `write_record` and `write_group_block`, so a closed stdout (the command piped into `head -1`) moved the record and left the queue stale; every print now follows the last file write (the same in `advance_waiting` and in the fence note). Not in `check_all` (a closed pipe is awkward to script); proven by hand. |
| 8 | Dropped middle chunk in a chain 1 → 2 → 3 (chunk 2 dropped while chunk 1 still open); "n of m"; group closing | **defect fixed** (chain), arithmetic held | Before: chunk 3's only dependency (chunk 2) was dropped, so chunk 3 was released while chunk 1 was still at `define.discuss` — the dependency it had through chunk 2 was lost. Fix: `blocking_deps` follows a dropped dependency's own `depends_on` (a dropped chunk hands its dependencies on): `wait moved: chunk 2 → chunk 1`, then refused; the group block's "waiting on" column follows the same rule (T170). Through the engine itself a middle chunk can only be dropped after its release, so the chain was already consistent (1 dropped → 2 released → 2 dropped → 3 released; `0 of 3 delivered; 2 dropped; in flight: chunk 3`); the hole was the hand-edited case and revisions. "n of m": `total` counts every child file, delivered and dropped included, so `1 of 3 delivered; 1 dropped` and `2 of 4 delivered` after a revision (the delivered chunk keeps its row and its old number, two rows may read "2"; documented in T133). The group closes only when delivered + dropped = total: `group closed: … 0 of 3 delivered; 3 dropped; nothing in flight` after all three dropped; `run.py --group` exits 0 with that line. |
| 9 | `--group` on a non-parent; on a closed group; on a parent still at `confirm-breakdown` | refused correctly / by design | A child: `stuck: … names a child (chunk 1 of …); pass its parent`, exit 4. A record at `define.sort` with no children: `stuck: … names a record with no children that is not at the breakdown (it is at define.sort) …`, exit 4. A closed group: `group closed: … 0 of 3 delivered; 3 dropped; nothing in flight`, exit 0. A parent at `confirm-breakdown`: the group loop runs the parent and stops at the PM gate (exit 3, "Then run again: … --group …") — by design, "one on its way there". |
| 10 | Impact section that omits a tagged dependant; tags in a nested folder (`code/backend/notify/deep/tests/test_digest.py`) and in a test file (`code/tests/test_mail.py`) | prose-only for the live challenger, executable in the fake; **prose gap fixed** | The fake's `define.challenge` reads every `@node` tag under `code/` (`rglob`, tests included) and wrote two findings: "the impact block omits the tagged dependant `digest` (@node digest in code/backend/notify/deep/tests/test_digest.py); list it with its options before approve (E20)" and the same for `weekly-mail`; the record stopped at `define.approve` with both in `## Findings — define.challenge`. For a live harness E20 is prose — and the prose was missing: `define.challenge`'s `what`/`notes` (what the runner puts in the prompt) never mentioned the impact section; only `framework/README.md` did. One sentence added to the step's `what` ("When the record carries an impact section, the challenge compares its dependants against the code tags (@node, every file under code/, tests included), the logic tree and the decision log, and reports every tagged node the section omits as a finding before approve"); `steps.md` and `studio.html` rebuilt (58 steps, layout unchanged). Nothing in `orchestrate.py` can decide which tagged node *is* a dependant of the removed one without the logic tree, so no gate was added: stated honestly as prose-only. |
| 11 | Removal whose dependant is mid-Deliver (E18), including a Deliver record with no `nodes:` front matter | **defect fixed** | The Phase 2 heuristic compared the two records' front-matter `nodes:` lists — a field no step, command or template ever writes (`change-record.md` lists every front-matter field; `nodes:` is not one), so in live use E18 could never fire and the gap was silent. Decision: widen to what the records do carry, and state it in the refusal. The removal record names a node in `nodes:`, in a task line's `[<id>]` tag, or in backticks in its body or its PM answers (the intake request lives in the history's `answer:`; the README's own worked example writes "remove `log-habit`"); the record in Deliver names a node in `nodes:` or in a task line `- [ ] Tnnn [P] [<id>] …` (the plan's documented grammar, present from `deliver.plan` on). Residual gap, stated in the refusal text and documented: a record at `deliver.trace` or `deliver.plan` with no task list yet, or one that names the node only in prose, is not compared (T152). Refusal: "changes/2026-09-10-8-build-login.md is in Deliver (deliver.execute) on node login, which this request names (E18); … (heuristic: …)". T150–T152; the Phase 4 case with `nodes:` still passes. |
| 12 | Stored data defaulted to delete anywhere | **defect fixed** | Two holes. (a) The `confirm-impact` answer had to name every dependant but not the store: "login: retire it too; …" was accepted with no word on `reset_password table`, and the handling of the store was left to the AI at `write` — delete by omission. Now `parse_impact` reads the `Stored data:` line(s) and the answer must carry `<store>: keep | migrate … | delete` for each: "the PM's answer names no choice for the stored data 'reset_password table' … (E19); … nothing is defaulted, delete least of all" (T140). (b) An impact block whose stored-data line reads `recommended: delete` could leave `impact`; now refused: ""delete" is never the default (E19) — recommend keep or migrate, the PM may still choose delete at define.confirm-impact" (T146). The fake recommends keep; the YAML notes said "never the default" in prose only. |
| 13 | Rule removal path (E17) | prose-only | `foundation.check-rule`'s notes (Phase 1) say the check writes the impact section against the delivered nodes that rely on the rule and the PM chooses at `propose-rule`; `foundation.impact` is a handoff with another meaning. Nothing in `orchestrate.py` or `run.py` recognises a rule removal, parses an impact section on a Foundation record, or refuses anything on that path; the fake harness writes no such block. Executable: nothing. This is honest to the plan (E17: "Foundation path … the PM chooses at Foundation's existing gate") and left as prose; a Foundation-side parser would be a new plan item. |
| 14 | The runner's "Most common answer" at `confirm-impact` pasted verbatim by a careless PM | refused correctly | The pasted command carries `--answer "<dependant>: retire it too \| keep it by <replacement> \| narrow the removal to <…> \| postpone; <store>: keep \| migrate \| delete"`; the gate looks for every dependant's id and every store's name in the answer, and the placeholder names none: "refused: the PM's answer names no choice for dependant 'login' …" (T139). It cannot skip the choice. Residual: a dependant literally named `dependant`, `replacement` or `store` would match the placeholder's words — improbable, accepted. At `confirm-breakdown` the most common answer is `yes`, which is what `spawn` needs; the PM reads the block at the stop. |
| 15 | Injection: a `## Breakdown — PM answer` heading with "yes" inside a proposed block; a chunk text with a heading; a slug with `..` or `/` | **defect fixed** (both) | The forged heading cut the proposed block short at that line (chunks after it were parsed as the "answer") and left a second answer block in the body; it could not reach `spawn` without a real yes edge (the gate's `advance` appends its own block, and the spawn-twice check needs the edge), so the gate held — but the proposal the PM read was truncated. Now the body may not carry more `PM answer` blocks than the gate has exits in the history: "carries 1 `## Breakdown — PM answer` block(s) but the PM has answered define.confirm-breakdown 0 time(s); that block is written only by `advance --answer` at the gate — remove the one written by hand or by the AI", at `breakdown`, at `impact` (`Impact — PM answer`) and at `spawn` (T160). A slug `../../etc/x` or `a/b` did not match the slug pattern and the chunk line was silently dropped from the parse (`_` was accepted); now any token is parsed as the slug and validated — "has the slug '../../etc/inventory-log', which cannot name a record file; a slug is lowercase letters, digits and hyphens (no `/`, `..`, `_` or spaces)" (T158). A chunk's text is one line, so a heading inside it cannot start a line; harmless. |
| 16 | Python 3.9 | held | No `python3.9` on this machine (3.11.15); `ast.parse(feature_version=(3,9))` over `framework/tools/*.py` and `tools/*.py` passes; no `match`, no runtime `X \| Y`, no walrus in the new code (`from __future__ import annotations` covers the `list[str]`/`tuple[…]` annotations); `Path.is_relative_to` is 3.9. `PYTHONDEVMODE=1 PYTHONWARNINGS=error python3 -X dev tools/check_all.py`: exit 0, no warning of any kind in 207 checks. |
| 17 | Docs vs code: every count reproduces; lint | held, one count corrected | Computed from the files after the fixes: 58 steps (define 25, deliver 19, foundation 14); 22 commands (1 meta, 8 entry; `check` prints "21 commands … 1 meta"); 22 templates; 12 components; `check_all` 207 `==` lines, 170 in step 8; test-case rows 396 in sections A–M and O–T (416 with section N's 20 re-run rows). `README.md` updated to 207 / 396 and the stage-8 row; `lint.py README.md` OK (27 headings, 1638 body words, every link resolves). Phase 5's "336 test cases" had counted section N's rows (416 − 80 = 336), while its own sentence says A–M and O–T; the badge and the two sentences now carry the count the sentence describes. `grep -rn "51 steps"` still returns only the historical lines. `framework/orchestration/README.md`, `change-record.md` and `framework/README.md` describe the new refusals and conventions; `DECISIONS.md` carries the Phase 6 paragraph. |

Also found and fixed, outside the list: the reopened `confirm-breakdown` (after `revise-breakdown`) accepted `--to write` ("one thing after all") and `--to drop` while the parent had four children in flight and delivered — the parent became a single node beside its group. Now refused while children exist ("from the reopened gate only spawn (a yes, reconciled) or breakdown (a rewrite) are legal — to stop the group, drop each chunk at its own gate"); a first-time gate still takes `write` (E1). T119–T120.

Not changed, on purpose: `revise-breakdown` while the parent is already reopened by another child (a second proposed block is appended; the PM sees the newest; the older child stays paused) — consistent with "a rewrite appends, never edits". The waiting child's release while its parent is reopened at `confirm-breakdown` — allowed; the revising child pauses, the others continue, as the plan says.

## Proof

```
$ python3 tools/validate.py
OK — 3 workflow(s), map consistent

$ python3 framework/tools/orchestrate.py check
CLOSED — 58 steps, every one mapped to a role and mode; 21 commands, every one entering a real non-routing step; 1 meta command entering the record's current step; toolbox: 3 step row(s)

$ python3 -c "import ast,glob; [ast.parse(open(f).read(),f,feature_version=(3,9)) for f in glob.glob('framework/tools/*.py')+glob.glob('tools/*.py')]; print('py39 syntax OK')"
py39 syntax OK

$ PYTHONDEVMODE=1 PYTHONWARNINGS=error python3 -X dev tools/check_all.py > check.log 2>&1; echo "exit $?"; tail -3 check.log; grep -c "^== " check.log; grep -c "^== group:" check.log; grep -ci warning check.log
exit 0
   refused: archify CLI not found; install with: python3 framework/tools/archify_delta.py install   (or: npx skills add tt-a1i/archify -g)

ALL CHECKS PASSED
207
170
0

$ python3 /mnt/user-data/outputs/github-readme-writer/lint.py README.md
OK — README.md: 27 headings, 1638 body words; every link and anchor resolves.

$ wc -l framework/tools/orchestrate.py tools/check_all.py workflows/define.yaml framework/test-cases.md
  1295 framework/tools/orchestrate.py
   601 tools/check_all.py
   509 workflows/define.yaml
   741 framework/test-cases.md
```

Must-fix findings open: 0.
